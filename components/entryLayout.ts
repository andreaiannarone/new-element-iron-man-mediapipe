/**
 * The free vertical band between the entry screen's top labels and its copy,
 * in viewport pixels. Measured by EntryScreen, read by the scene to size and
 * centre the sphere in it on portrait screens.
 */
export const entrySphereSlot = { top: 0, bottom: 0, valid: false };
